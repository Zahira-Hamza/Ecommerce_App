//! here we can filter the data depending on the presentation but not in the original response(DTO)

class Category {
  final String? id;

  final String? name;

  // final String? slug;

  final String? image;
  //
  // final String? createdAt;
  //
  // final String? updatedAt;

  Category({
    this.id,
    this.name,
    this.image,
  });
}
